This is a waybar config heavily based on joseans one 

dependences
sudo pacman -S waybar ttf-dejavu ttf-font-awesome

install

git clone [https://github.com/juanz-btw/waybar.git](https://github.com/juanz-btw/waybar.git) ~/.config/waybar



Bloque de código (fenced) — añade el lenguaje después de los tres backticks para resaltado:

# clonar e instalar
git clone https://github.com/usuario/repositorio.git
cd repositorio
npm install

Bloque de código en Python:

